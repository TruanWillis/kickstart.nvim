-- Git signs in the gutter. Hunk navigation and staging are not mapped here;
-- see `:help gitsigns-functions` if you want them.
return {
  'lewis6991/gitsigns.nvim',
  opts = {
    signs = {
      add = { text = '+' },
      change = { text = '~' },
      delete = { text = '_' },
      topdelete = { text = '‾' },
      changedelete = { text = '~' },
    },
  },
}
